server:
  port: 10019

spring:
  profiles:
    active: dev
  application:
    name: magic-bag-product
  config:
    import:
      - nacos:${spring.application.name}-${spring.profiles.active}.yaml?group=${spring.application.name}&refresh=true
      - nacos:shared-config.yaml?refresh=true

  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 52.77.254.95:8848  # Nacos服务注册地址
        group: MAGIC_BAG
      config:
        server-addr: 52.77.254.95:8848  # Nacos配置中心地址